[loggers]
keys=root

[logger_root]
level=INFO
handlers=console

[handlers]
keys=console

[handler_console]
class=logging.StreamHandler
level=INFO
formatter=console
args=(sys.stderr,)

[formatters]
keys=console

[formatter_console]
format=[%(asctime)s] %(levelname)8s %(name)s: %(message)s
