# This file is automatically loaded by pytest.
#
# It must be there to let pytest know where the root directory of
# the project is (to set PYTHONPATH accordingly), so the `test.data`
# module could be imported in tests.
